torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Diffusion & Transformers
# NOTE: diffusers 0.36+ requires transformers < 5.0 for compatibility.
diffusers>=0.36.0,<0.40.0
transformers>=4.35.0,<5.0.0
accelerate>=0.25.0,<1.0.0
safetensors>=0.4.0

# Quantization & LoRA (optional fine-tuning)
bitsandbytes
peft

# Video processing
av
ffmpeg-python
opencv-python
imageio
imageio-ffmpeg

# Metrics
lpips
scikit-image
torch-fidelity

# Audio (TTS bonus)
librosa
soundfile
pydub

# Utilities
matplotlib
seaborn
pandas
tqdm
ftfy
regex
pyyaml

# Jupyter (optional)
ipykernel
